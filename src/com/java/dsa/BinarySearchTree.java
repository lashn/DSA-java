package com.java.dsa;

public class BinarySearchTree {
    public static void main(String[] args) {
        /* i=10
        bst=[5,8,9,15] 8 is the root
        insert(root Node,int x){
            if root==null
                return new Node(x);
            if (x>root.key)
                root.right = insert(root.right,x);
            if (x<root.key)
                root.left = insert(root.left,x);
            return root

                return
             if (
         */
    }
}
